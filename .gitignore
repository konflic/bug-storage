__pycache__/
*.pyc
.venv/
venv/
.env
data/
*.db
*.db-wal
*.db-shm

# Secrets
key.txt

# Local config with real credentials / IPs
opencode.json

# Tracker API spec (contains internal endpoints; keep only the .example)
tracker-openapi.json

# Local backups
backups/

# Deploy secrets
deploy/.env.prod
