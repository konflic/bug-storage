fastapi==0.138.1
starlette==1.3.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
# MCP server so an analysis agent can call the bug DB natively.
mcp==1.2.0
sse-starlette==3.4.5
# Postgres driver (used only when DATABASE_URL points to postgres). Harmless to install now.
psycopg[binary]==3.2.3
